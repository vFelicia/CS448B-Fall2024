four lines of python code to extract extension from the file name i'll run this code you can see the path and the extension if you want the file extension only then we can use index one now again let's run this code you can see we get only the file extension txt