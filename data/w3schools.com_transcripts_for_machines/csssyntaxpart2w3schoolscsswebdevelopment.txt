let's put it all together watch how we transform a plain HTML page into a visually appealing design using CSS syntax first we select the body element our goal to change its background color with a simple line of CSS body background color light blue the background transforms next let's style our header we want it centered on the page here's how we do it using H1 text align center now our headers is centered with each line of CSS our page becomes more vibrant and engaging and that's the magic of CSS syntax