imagine you're painting a picture instead of using just plain paint you decide to start with a patterned canvas that's exactly what the background image property does in CSS it lets you specify an image to use as the background of an element let's start simple to set a background image for an entire page you can add this to your CSS file background image URL paper. GI by default the image repeats itself to cover the whole element it's like a tile floor where the same tile is laid out over and over again until the whole floor is covered