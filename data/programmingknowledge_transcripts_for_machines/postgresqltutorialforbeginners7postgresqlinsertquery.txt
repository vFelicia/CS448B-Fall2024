and in this video I will show you how to insert there is those tables so first of all open your terminal right psq well here you can see we have test database we are going to use that and to connect the database right /c in the name of the database now we are connected to be tested does create a storm table okay table the name of the table Sona brackets now specifying the columns overall number and the land is now the primary key and so these stones as unique roll number we will say floor number as a primary key okay our paper is created and let us see the table class P and you can see okay so to insert data into this table Inc the name of the table then column names in which you want to insert the values that value here you can provide value 0 1 this is African insert values in together okay so let's click the terminal you okay here we have a database let us try to insert some values in this table okay mmm here is one thing if you wanted to insert all the values for the all the values for all the columns it means that for every column you have in value which you wanted to you want to insert so in this case you don't need to write the column names you can directly start with the values okay one row is inserted this is how you can insert as many as value you want we can also insert multiple values in order to do that just put comma here and then write another value let us change this value robot and you can see two values and Phoenix itself so these are we can insert the multiple values in the table and remember one thing whenever you specify the column names here like this and then you have to insert the values in respective order means you take if you write row number first and Ptolemaic up first and you write name second then roll then name comes second okay so order is very important okay so